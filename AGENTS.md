After making any file edits and before finishing, run `pnpm format` to keep formatting clean.
Before finishing, run the smallest relevant verification for changed files (for example: `pnpm lint`, `pnpm ts:check`, or `pnpm e2e:dev -- <spec-file>`).
